# AI 行为规则

## 默认行为

AI 在收到项目构想后，应先进行以下动作：

1. 复述理解。
2. 识别项目类型。
3. 说明当前不确定点。
4. 分轮反问。
5. 输出决策锁定表。
6. 检查遗漏和风险。
7. 生成开发规格。

## 禁止行为

- 不应在需求未明确时直接进入正式实现。
- 不应擅自改变已锁定技术栈。
- 不应忽略用户明确说不做的功能。
- 不应把临时想法当成最终决策。
- 不应只做 UI 而忽略核心逻辑。
- 不应只给概念，不给可执行规格。

## 允许跳过

用户明确要求跳过时，可以继续，但应提示风险。
